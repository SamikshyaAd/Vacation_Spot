Description: >
  Vacation_Spot
  This template defines the ECR

Parameters:
  RepositoryName:
    Type: String
    Description: ECR Repository name

Resources:
  ElasticContainerRegistry:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: !Ref RepositoryName